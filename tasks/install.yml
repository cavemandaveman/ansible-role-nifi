---
- name: Download NiFi binary
  get_url:
    url: "{{ download_mirror_uri }}/nifi/{{ nifi_version }}/nifi-{{ nifi_version }}-bin.tar.gz"
    dest: "/tmp/nifi-{{ nifi_version }}-bin.tar.gz"

- name: Unpack NiFi binary
  unarchive:
    remote_src: "yes"
    src: "/tmp/nifi-{{ nifi_version }}-bin.tar.gz"
    dest: "{{ config_dirs.nifi_install }}/"
    owner: "{{ nifi_user }}"
    group: "{{ nifi_group }}"
    creates: "{{ config_dirs.nifi_install }}/nifi-{{ nifi_version }}"

- name: Fix permissions
  file:
    path: "{{ config_dirs.nifi_install }}/nifi-{{ nifi_version }}"
    owner: "{{ nifi_user }}"
    group: "{{ nifi_group }}"
    recurse: "yes"

- name: Create NiFi symlink
  file:
    src: "nifi-{{ nifi_version }}"
    path: "{{ config_dirs.nifi_home }}"
    state: "link"
    owner: "{{ nifi_user }}"
    group: "{{ nifi_group }}"
